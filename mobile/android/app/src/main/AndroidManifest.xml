<manifest xmlns:android="http://schemas.android.com/apk/res/android">

<<<<<<< Updated upstream
    <!-- Mạng (tải ảnh QR, gọi API) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Vị trí -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Lưu/đọc ảnh theo phiên bản Android -->
    <!-- Android 13+ (API 33+): đọc ảnh trong MediaStore -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Android 10–12 (API 29–32): đọc bộ nhớ ngoài -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Android 9 trở xuống (API 28-): ghi bộ nhớ ngoài -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:label="NEX"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
=======
    <!-- ✅ KHAI BÁO QUYỀN (đặt trong <manifest> nhưng ngoài <application>) -->
    <!-- Quyền camera để chụp ảnh -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Quyền đọc ảnh trên Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Nếu bạn có chọn/quay video thì bật thêm dòng sau:
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    -->

    <!-- Quyền đọc bộ nhớ cho Android 12 trở xuống -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- ⬇️ PHẦN ỨNG DỤNG -->
    <application
        android:label="NEX"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
>>>>>>> Stashed changes

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

<<<<<<< Updated upstream
=======
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
>>>>>>> Stashed changes
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

<<<<<<< Updated upstream
        <!-- Flutter plugin registry -->
=======
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
>>>>>>> Stashed changes
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

<<<<<<< Updated upstream
    <!-- Package visibility cho ProcessTextPlugin (Flutter) -->
=======
    <!-- Giữ nguyên phần queries mặc định của Flutter (dành cho ProcessTextPlugin) -->
>>>>>>> Stashed changes
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
