<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Quyền mạng (tải ảnh QR) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Quyền camera (bạn đang dùng sẵn) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Quyền vị trí (bạn đang dùng sẵn) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Lưu ảnh vào thư viện (tuỳ phiên bản Android) -->
    <!-- Android 13+ (API 33): đọc ảnh trong MediaStore -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Android 10–12 (API 29–32): đọc bộ nhớ ngoài -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Android 9 trở xuống: quyền ghi (thư viện saver sẽ tự dùng MediaStore khi có thể) -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:label="NEX"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"

        <!-- Cho phép HTTP cleartext (phục vụ dev với http://10.0.2.2) -->
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Flutter plugin registry -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Package visibility cho ProcessTextPlugin (Flutter) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
